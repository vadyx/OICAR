export const profileImageOptions = {
    base64: true,
    allowsEditing: true,
    aspect: [1, 1],
    quality: 0.5
};

export const fullSizeImageOptions = {
    base64: true,
    allowsEditing: true
};